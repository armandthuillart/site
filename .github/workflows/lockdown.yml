name: 'Lockdown'

on:
  pull_request_target:
    types: opened

permissions:
  pull-requests: write

jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - name: Repo Lockdown
        uses: dessant/repo-lockdown@v5.0.0
        with:
          pr-comment: 'This project does not accept pull requests. Please use issues to report bugs or suggest new features.'
          process-only: 'prs'
